export function render(
    selector: string,
    position: InsertPosition,
    template: string
) {
    const validPosition = [
        'beforeend',
        'beforebegin',
        'afterend',
        'afterbegin',
    ];

    if (!validPosition.includes) {
    }
}
